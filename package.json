{
  "name": "vjc-makers.github.io",
  "private": true,
  "devDependencies": {
    "@alrra/travis-scripts": "^3.0.0"
  },
  "scripts": {
    "build": "jekyll build --config _build/_config.yml",
    "build:production": "jekyll build --config _build/_config.yml,_build/_config.production.yml",
    "start": "jekyll serve --config _build/_config.yml"
  }
}
